{
  "name": "tuff-core",
  "type": "module",
  "files": [
    "dist"
  ],
  "version": "0.3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Terrier-Tech/tuff"
  },
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && cp -r types/*.d.ts dist",
    "preview": "vite preview",
    "prepare": "tsc && vite build && cp -r types/*.d.ts dist",
    "tsc": "tsc",
    "gen": "node --loader ts-node/esm  --experimental-modules --experimental-specifier-resolution=node src/gen/dom.ts",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/node": "^17.0.0",
    "@vanilla-extract/css": "^1.6.8",
    "@vanilla-extract/vite-plugin": "^3.1.2",
    "rollup-plugin-folder-input": "^1.0.1",
    "sass": "^1.45.0",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5",
    "vite": "^2.7.0",
    "vitest": "^0.6.0"
  },
  "module": "./dist/tuff.es.js",
  "types": "./dist/main.d.ts",
  "exports": {
    ".": {
      "import": "./dist/tuff.es.js"
    }
  }
}
